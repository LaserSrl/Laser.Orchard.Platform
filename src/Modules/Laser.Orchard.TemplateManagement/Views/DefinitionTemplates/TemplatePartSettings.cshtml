@model Laser.Orchard.TemplateManagement.Settings.TemplatePartSettings
@{
	Style.Include("Common.css", "Common.min.css");
}
<fieldset>
	<div class="editor-field">
		@Html.LabelFor(m => m.DefaultParserIdSelected, T("Default Parser"))
		@Html.EditorFor(m => m.DefaultParserIdSelected)
		<span class="hint">@T("The template parser engine to use by default.")</span>
	</div>
</fieldset>
