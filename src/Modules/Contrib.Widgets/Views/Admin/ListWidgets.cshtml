@{
    Style.Require("TheAdminWidgetContainerStyle");

    Layout.Title = "Choose A Widget";
}
<ul id="available-widgets">
    @foreach (var widgetType in Model.WidgetTypes) {
        <li><a href="@Url.Action("AddWidget", "Admin", new { area = "Contrib.Widgets", hostId = Model.HostId, widgetType, zone = Model.Zone })">@widgetType</a></li>
    }
</ul>